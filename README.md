# CASE-STUDY-4
## Analyze a log file to count keywords, filter logs into files, enable search, and use seek operations.
Read a given log file and perform operations such as counting occurrences of “INFO”, “WARNING”, and “ERROR”, storing filtered logs into separate files, enabling keyword-based search, and demonstrating file pointer movements using seek().
